请为我开发一款 Windows 桌面端 快速IP切换工具，使用 **Wails v2 + Go** 技术栈开发，带现代化美观GUI界面，支持一键切换静态IP/DHCP、保存/加载历史网络配置、本地持久化存储，功能完整、可直接编译运行、无bug。

### 核心功能需求
1. 网卡管理
- 自动读取本地所有以太网、Wi-Fi网络适配器
- 下拉框选择目标网卡，支持一键刷新网卡列表
- 显示网卡名称、设备信息

2. IP配置切换
- 支持 静态IP / DHCP自动获取 一键切换
- 静态模式可填写：IP地址、子网掩码、网关、首选DNS、备用DNS
- DHCP模式一键清空静态配置，自动获取网络

3. 配置保存与历史记录
- 支持自定义配置名称（如：家庭、公司、宿舍）
- 一键保存当前配置到本地
- 下拉框加载历史配置，一键填充到界面
- 配置文件持久化存储，重启软件不丢失

4. 一键应用
- 点击应用按钮自动修改网卡网络配置
- 实时显示操作日志、成功/失败提示
- 管理员权限自动校验，无权限弹窗提醒

5. 界面要求
- 现代化简洁UI，中文界面，响应式布局
- 操作简单，适合电脑新手
- 按钮、输入框布局清晰，视觉舒适
- Windows原生风格体验

### 技术栈严格要求
- 前端：Wails v2 + HTML + CSS + JavaScript（原生，无需框架）
- 后端：Go 1.18+
- 配置存储：JSON文件本地存储
- 系统调用：Windows netsh 命令
- 编译：支持打包为 Windows 原生exe

### 项目结构要求
- 完整main.go 后端逻辑
- 完整前端界面（index.html + app.js + app.css）
- Go 实现：网卡获取、IP切换、配置读写、权限判断
- 前端实现：GUI交互、数据绑定、提示、日志展示
- 前后端通过 Wails 绑定通信

### 代码必须满足
- 完整可直接编译运行，无缺失文件
- 注释清晰，结构规范
- 输入格式校验（IP地址合法性）
- 错误捕获与友好提示
- 支持管理员权限运行

请直接输出完整项目代码，包含：
1. main.go
2. frontend/index.html
3. frontend/app.js
4. frontend/app.css
并附带编译运行命令、使用说明！